import datetime
from typing import Optional

# from sqlalchemy import Column, Integer, String, ForeignKey, DateTime, Date
# from sqlalchemy.ext.declarative import declarative_base

# BaseModel = declarative_base()

# from sqlmodel import SQLModel, Field, Integer, String
#
#
# class Users(SQLModel, table=True):
#     id: Optional[int] = Field(primary_key=True)
#     name: str = Field(String(50), unique=True, index=True, nullable=False)
#     role: str
#     email: str = Field(String(120), unique=True, index=True, nullable=False)

# password: str =
# age: int = Column(Integer, nullable=False)
# created_at: DateTime = Column(DateTime, default=datetime.datetime.now)
# updated_at: DateTime = Column(DateTime, default=datetime.datetime.now, onupdate=datetime.datetime.now)
# deleted_at: Date = Column(Date, default=None)
#
